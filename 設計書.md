# レシート読み込み家計簿アプリ 設計書

## 1. 概要

レシート画像をアップロードするだけで、Claude AIが内容を自動読み取りし、カテゴリ別に分類・集計する家計簿Webアプリケーション。

---

## 2. システム構成

```
ブラウザ (React)
    ↕ HTTP (localhost:3000 → proxy → 3001)
バックエンド (Node.js / Express)
    ↕ HTTPS
Claude API (Anthropic)
```

### ディレクトリ構成

```
C:\temp\test\
├── .env                        # 環境変数（APIキー）※Git管理外
├── .gitignore
├── CLAUDE.md                   # Claude Code向けプロジェクト設定
├── 設計書.md                   # 本ドキュメント
├── package.json                # ルート：concurrently でサーバー+クライアント同時起動
├── server/
│   ├── package.json
│   └── server.js               # Express サーバー / Claude API 呼び出し
└── client/
    ├── package.json            # proxy: http://localhost:3001
    ├── public/
    │   └── index.html
    └── src/
        ├── index.js
        ├── index.css
        ├── App.js              # メイン：タブ管理・ローカルストレージ
        ├── App.css
        └── components/
            ├── ReceiptUpload.js    # 画像アップロード・解析トリガー
            ├── ReceiptUpload.css
            ├── ItemList.js         # レシート明細一覧
            ├── ItemList.css
            ├── Summary.js          # カテゴリ別サマリー
            ├── Summary.css
            ├── Charts.js           # 円グラフ・月別棒グラフ
            └── Charts.css
```

---

## 3. 技術スタック

| 層 | 技術 | バージョン | 用途 |
|---|---|---|---|
| フロントエンド | React | 18.3.1 | UIフレームワーク |
| グラフ | Chart.js + react-chartjs-2 | 4.4.3 / 5.2.0 | 円グラフ・棒グラフ描画 |
| バックエンド | Node.js + Express | 4.19.2 | APIサーバー |
| ファイル受信 | multer | 1.4.5-lts | マルチパートフォーム処理 |
| AI解析 | @anthropic-ai/sdk | 0.36.3 | Claude API クライアント |
| 使用モデル | claude-haiku-4-5-20251001 | - | レシート画像解析 |
| 環境変数 | dotenv | 16.4.5 | APIキー管理 |
| 同時起動 | concurrently | 8.2.2 | サーバー+クライアント並行起動 |

---

## 4. 機能一覧

### 4.1 レシート登録（ReceiptUpload）

- ドラッグ&ドロップまたはクリックで画像選択
- 対応形式：JPG / PNG / GIF / WebP（最大10MB）
- プレビュー表示後、「AIで解析する」ボタンで送信
- バックエンド経由で Claude API に Base64エンコード画像を送信
- 解析結果を自動でローカルストレージに保存

### 4.2 明細一覧（ItemList）

- 登録済みレシートをカード形式で表示
- カードをクリックで明細（商品名・カテゴリ・金額）を展開表示
- レシート単位での削除
- 全データの一括削除

### 4.3 カテゴリ別サマリー（Summary）

- 合計支出金額の表示
- カテゴリ別金額・割合・プログレスバーを表示
- 金額降順にソート

### 4.4 グラフ分析（Charts）

- **円グラフ**：カテゴリ別支出の割合
- **棒グラフ**：月別支出の推移（直近6ヶ月）

### 4.5 データ永続化

- ローカルストレージ（キー：`kakeibo_receipts`）に JSON 保存
- ページリロード・ブラウザ再起動後もデータを保持

---

## 5. カテゴリ定義

| カテゴリ | アイコン | 色 |
|---|---|---|
| 食費 | 🛒 | #4ade80 |
| 外食 | 🍽️ | #f97316 |
| 日用品 | 🧴 | #60a5fa |
| 交通費 | 🚃 | #a78bfa |
| 医療費 | 💊 | #f43f5e |
| 娯楽 | 🎮 | #fbbf24 |
| その他 | 📦 | #94a3b8 |

---

## 6. API仕様

### POST `/api/analyze-receipt`

レシート画像を受け取り、Claude APIで解析して構造化データを返す。

**リクエスト**

| 項目 | 内容 |
|---|---|
| Content-Type | multipart/form-data |
| フィールド名 | `receipt` |
| ファイル種別 | image/* |
| 最大サイズ | 10MB |

**レスポンス（成功時）**

```json
{
  "date": "2026-06-28",
  "store": "イオン",
  "items": [
    { "name": "牛乳", "price": 198, "category": "食費" },
    { "name": "シャンプー", "price": 498, "category": "日用品" }
  ],
  "total": 696
}
```

**エラーレスポンス**

```json
{ "error": "エラーメッセージ" }
```

---

## 7. データ構造

### ローカルストレージ保存形式

```json
[
  {
    "id": 1719532800000,
    "date": "2026-06-28",
    "store": "店舗名",
    "items": [
      { "name": "商品名", "price": 198, "category": "食費" }
    ],
    "total": 198,
    "createdAt": "2026-06-28T00:00:00.000Z"
  }
]
```

---

## 8. セキュリティ

- Claude APIキーは `.env` で管理し、`.gitignore` でGit管理外
- APIキーはバックエンドのみが参照（ブラウザに露出しない）
- CORS は `http://localhost:3000` のみ許可
- アップロードは画像ファイルのみ受け付けるバリデーションあり

---

## 9. 起動方法

```bash
# 初回セットアップ
npm run install:all

# .envにAPIキーを設定
ANTHROPIC_API_KEY=sk-ant-xxxxx

# 開発サーバー起動（サーバー+クライアント同時）
npm run dev
```

| サーバー | URL |
|---|---|
| フロントエンド | http://localhost:3000 |
| バックエンド | http://localhost:3001 |

---

## 10. リポジトリ

- **GitHub**: https://github.com/yusa0903/test.git
- **ブランチ**: `main`

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-06-28 | 初版作成 |
