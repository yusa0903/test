# レシート読み込み家計簿アプリ 設計書

## 1. 概要

レシート画像をアップロードするだけで、Claude AIが内容を自動読み取りし、カテゴリ別に分類・集計する家計簿Webアプリケーション。Supabase認証によりユーザーごとにデータを分離して管理する。

---

## 2. ディレクトリ構成

```
C:\temp\test\
├── .env                        # 環境変数（APIキー・Supabaseキー）※Git管理外
├── .gitignore
├── CLAUDE.md                   # Claude Code向けプロジェクト設定
├── 設計書.md                   # 本ドキュメント
├── package.json                # ルート：concurrently でサーバー+クライアント同時起動
├── server/
│   ├── package.json
│   └── server.js               # Express サーバー / Claude API 呼び出し / JWT認証
├── supabase/
│   └── schema.sql              # DBスキーマ・RLSポリシー定義
└── client/
    ├── .env                    # REACT_APP_SUPABASE_URL / ANON_KEY ※Git管理外
    ├── package.json            # proxy: http://localhost:3001
    ├── public/
    │   └── index.html
    └── src/
        ├── index.js            # AuthProviderでラップ
        ├── index.css
        ├── App.js              # メイン：タブ管理・Supabase連携
        ├── App.css
        ├── lib/
        │   └── supabase.js         # Supabaseクライアント初期化（anon_key）
        ├── contexts/
        │   └── AuthContext.js      # 認証状態・ロール管理
        └── components/
            ├── Login.js            # ログイン・新規登録画面
            ├── Login.css
            ├── ReceiptUpload.js    # 画像アップロード・進捗バー・解析トリガー
            ├── ReceiptUpload.css
            ├── ItemList.js         # レシート明細一覧（管理者表示対応）
            ├── ItemList.css
            ├── Summary.js          # カテゴリ別サマリー
            ├── Summary.css
            ├── Charts.js           # 円グラフ・月別棒グラフ・積み上げ棒グラフ
            └── Charts.css
```

---

## 3. システム構成図

```
ブラウザ (React SPA)
    │  ① メール/パスワードでログイン
    ↕  Supabase Auth（直接通信）
    │  ② JWTアクセストークン取得
    │
    ↕  HTTP + Authorization: Bearer <JWT>
    │  （localhost:3000 → proxy → 3001）
    │
バックエンド (Node.js / Express)
    │
    ├─ [JWT検証・管理者操作]
    │   supabase（service_role_key）
    │   - supabase.auth.getUser(token) でJWT検証
    │   - user_rolesテーブルで管理者判定
    │   - 管理者の場合はRLSを迂回して全件操作
    │
    ├─ [一般ユーザーのデータ操作]
    │   createUserClient(token)（anon_key + ユーザーJWT）
    │   - RLSポリシー auth.uid() = user_id をDBレベルで適用
    │   - 自分のデータのみ取得・挿入・削除可能
    │
    └─ [レシート解析]
        Claude API（Anthropic）
        - claude-haiku-4-5-20251001 で画像解析
```

---

## 4. 技術スタック

| 層 | 技術 | バージョン | 用途 |
|---|---|---|---|
| フロントエンド | React | 18.3.1 | UIフレームワーク |
| グラフ | Chart.js + react-chartjs-2 | 4.4.3 / 5.2.0 | 円グラフ・棒グラフ描画 |
| 認証 | Supabase Auth | - | メール/パスワード認証・JWT発行 |
| DB | Supabase PostgreSQL | - | レシートデータ永続化・RLS |
| バックエンド | Node.js + Express | 4.19.2 | APIサーバー |
| ファイル受信 | multer | 1.4.5-lts | マルチパートフォーム処理 |
| AI解析 | @anthropic-ai/sdk | 0.36.3 | Claude API クライアント |
| 使用モデル | claude-haiku-4-5-20251001 | - | レシート画像解析 |
| 環境変数 | dotenv | 16.4.5 | APIキー管理 |
| 同時起動 | concurrently | 8.2.2 | サーバー+クライアント並行起動 |

---

## 5. 認証・認可設計

### 5.1 認証フロー

1. ユーザーがメール/パスワードを入力 → Supabase Auth で認証
2. Supabase が JWT アクセストークンを発行
3. フロントエンドは全APIリクエストに `Authorization: Bearer <token>` を付与
4. バックエンドは `supabase.auth.getUser(token)` でトークンを検証し `req.user` / `req.token` にセット

### 5.2 ロール管理

| ロール | 参照範囲 | 削除権限 |
|---|---|---|
| user（一般） | 自分のレシートのみ | 自分のレシートのみ |
| admin（管理者） | 全ユーザーのレシート | 全ユーザーのレシート |

管理者の付与は Supabase SQL Editor から手動で実行：
```sql
insert into user_roles (user_id, role) values ('<uuid>', 'admin');
```

### 5.3 データ分離の実装方針

一般ユーザーのデータ操作は **anon_key + ユーザーJWT** で生成したクライアント（`createUserClient`）を使用。SupabaseのRLSポリシーがDBレベルで適用されるため、アプリ層のフィルタリングに依存せず確実にデータを分離する。

| 操作 | 使用クライアント | 理由 |
|---|---|---|
| JWT検証（`auth.getUser`） | service_role | 管理者権限が必要 |
| ロール取得（`user_roles`） | service_role | RLSで自分のロールのみ参照可能なため管理者操作で迂回 |
| 管理者のデータ全件取得 | service_role | RLSを迂回して全ユーザーのデータを参照 |
| 一般ユーザーのデータ取得 | createUserClient（RLS有効） | `auth.uid() = user_id` でDB側で分離 |
| レシート挿入 | createUserClient（RLS有効） | 自分のuser_idにのみ挿入を許可 |
| 一般ユーザーの削除 | createUserClient（RLS有効） | 自分のデータのみ削除を許可 |
| 管理者の削除 | service_role | 任意のレシートを削除可能 |

### 5.4 環境変数

| 変数名 | 用途 | 設定ファイル |
|---|---|---|
| `ANTHROPIC_API_KEY` | Claude API認証 | `.env`（サーバー） |
| `SUPABASE_URL` | SupabaseプロジェクトURL | `.env`（サーバー） |
| `SUPABASE_SERVICE_ROLE_KEY` | JWT検証・管理者操作（RLS迂回） | `.env`（サーバー） |
| `SUPABASE_ANON_KEY` | ユーザー固有クライアント生成（RLS有効） | `.env`（サーバー） |
| `REACT_APP_SUPABASE_URL` | フロントSupabase接続 | `client/.env` |
| `REACT_APP_SUPABASE_ANON_KEY` | フロントSupabase認証 | `client/.env` |

---

## 6. 機能一覧

### 6.1 認証（Login）

- メールアドレス/パスワードによるログイン
- 新規アカウント登録
- ログアウト
- 未ログイン時はログイン画面のみ表示

### 6.2 レシート登録（ReceiptUpload）

- ドラッグ&ドロップまたはクリックで画像選択
- 対応形式：JPG / PNG / GIF / WebP（最大10MB）
- プレビュー表示後、「AIで解析する」ボタンで送信
- 解析中は4ステップの進捗バーをアニメーション表示
- バックエンド経由で Claude API に Base64エンコード画像を送信
- 解析結果を Supabase DB に保存（ログインユーザーのIDに紐付け）

### 6.3 明細一覧（ItemList）

- 登録済みレシートをカード形式で表示
- カードをクリックで明細（商品名・カテゴリ・金額）を展開表示
- レシート単位での削除
- 全データの一括削除
- 管理者は登録者のメールアドレスを表示

### 6.4 カテゴリ別サマリー（Summary）

- 合計支出金額の表示
- カテゴリ別金額・割合・プログレスバーを表示
- 金額降順にソート

### 6.5 グラフ分析（Charts）

- **円グラフ**：カテゴリ別支出の割合
- **積み上げ棒グラフ**：日付別支出の内訳（直近30日・カテゴリ別）
- **棒グラフ**：月別支出の推移（直近6ヶ月）

### 6.6 データ永続化

- Supabase PostgreSQL に保存
- ログインユーザーのIDに紐づいてデータが永続化される
- RLSにより他ユーザーからのアクセスをDBレベルで遮断

---

## 7. カテゴリ定義

| カテゴリ | アイコン | 色 |
|---|---|---|
| 食費 | 🛒 | #4ade80 |
| 外食 | 🍽️ | #f97316 |
| 日用品 | 🧴 | #60a5fa |
| 交通費 | 🚃 | #a78bfa |
| 医療費 | 💊 | #f43f5e |
| 娯楽 | 🎮 | #fbbf24 |
| その他 | 📦 | #94a3b8 |

---

## 8. API仕様

すべてのエンドポイントは `Authorization: Bearer <jwt>` ヘッダーが必要。

### GET `/api/me`
ログインユーザーの情報とロールを返す。

**レスポンス**
```json
{ "id": "uuid", "email": "user@example.com", "role": "user" }
```

### GET `/api/receipts`
レシート一覧を返す。管理者は全件、一般ユーザーはRLSにより自分のデータのみ。

**レスポンス**
```json
{ "receipts": [...], "isAdmin": false }
```

### POST `/api/receipts`
解析済みレシートデータをDBに保存する。

**リクエストボディ**
```json
{ "date": "2026-06-28", "store": "店舗名", "items": [...], "total": 1000 }
```

### DELETE `/api/receipts/:id`
指定IDのレシートを削除する。一般ユーザーはRLSにより自分のデータのみ削除可能。

### DELETE `/api/receipts`
自分のレシートを全件削除する。RLSにより自分のデータのみ対象。

### POST `/api/analyze-receipt`

レシート画像を受け取り、Claude APIで解析して構造化データを返す。

**リクエスト**

| 項目 | 内容 |
|---|---|
| Content-Type | multipart/form-data |
| フィールド名 | `receipt` |
| ファイル種別 | image/* |
| 最大サイズ | 10MB |

**レスポンス（成功時）**

```json
{
  "date": "2026-06-28",
  "store": "イオン",
  "items": [
    { "name": "牛乳", "price": 198, "category": "食費" },
    { "name": "シャンプー", "price": 498, "category": "日用品" }
  ],
  "total": 696
}
```

**エラーレスポンス**

```json
{ "error": "エラーメッセージ" }
```

---

## 9. データモデル

### receipts テーブル（Supabase PostgreSQL）

| カラム | 型 | 説明 |
|---|---|---|
| id | uuid (PK) | 自動生成 |
| user_id | uuid (FK: auth.users) | 登録者のユーザーID（NOT NULL） |
| user_email | text | 登録者のメールアドレス |
| date | text | レシート日付（YYYY-MM-DD） |
| store | text | 店舗名 |
| items | jsonb | 商品リスト（name / price / category） |
| total | numeric | 合計金額 |
| created_at | timestamptz | 登録日時（デフォルト: now()） |

### RLSポリシー（receipts）

| ポリシー | 条件 |
|---|---|
| SELECT | `auth.uid() = user_id` |
| INSERT | `auth.uid() = user_id` |
| DELETE | `auth.uid() = user_id` |

### user_roles テーブル

| カラム | 型 | 説明 |
|---|---|---|
| user_id | uuid (PK, FK: auth.users) | ユーザーID |
| role | text | ロール（default: 'user'） |
| created_at | timestamptz | 作成日時 |

---

## 10. セキュリティ

- Claude APIキーは `.env` で管理し、`.gitignore` でGit管理外
- `SUPABASE_SERVICE_ROLE_KEY` はサーバーのみ（`.env`・Git管理外）
- `SUPABASE_ANON_KEY` はサーバー側でユーザー固有クライアント生成に使用（Git管理外）
- `REACT_APP_SUPABASE_ANON_KEY` はクライアント用（公開可・RLSで保護）
- APIキーはバックエンドのみが参照（ブラウザに露出しない）
- CORS は `http://localhost:3000` のみ許可
- 全APIエンドポイントはJWT認証必須
- Supabase RLS でDBレベルのデータ分離を実施
- 社内プロキシ対応：`NODE_TLS_REJECT_UNAUTHORIZED=0`（開発環境のみ）

---

## 11. 起動方法

```bash
# 1. Supabase でプロジェクト作成後、supabase/schema.sql を SQL Editor で実行

# 2. 環境変数を設定
# .env（サーバー用）
ANTHROPIC_API_KEY=sk-ant-xxxxx
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJxx...   # Project Settings > API > service_role > secret
SUPABASE_ANON_KEY=sb_publishable_xx  # Project Settings > API > anon > public

# client/.env（フロントエンド用）
REACT_APP_SUPABASE_URL=https://xxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=sb_publishable_xx

# 3. パッケージインストール
npm run install:all
cd server && npm install

# 4. 開発サーバー起動
npm run dev
```

| サーバー | URL |
|---|---|
| フロントエンド | http://localhost:3000 |
| バックエンド | http://localhost:3001 |

---

## 12. リポジトリ

- **GitHub**: https://github.com/yusa0903/test.git
- **ブランチ**: `main`

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-06-28 | 初版作成 |
| 2026-06-28 | 進捗バーをレシート解析中に表示する機能を追加 |
| 2026-06-28 | 日付別カテゴリ積み上げ棒グラフをグラフ分析に追加 |
| 2026-06-28 | Supabase認証認可機能を追加・データ保存先をSupabase DBに移行 |
| 2026-06-28 | データ分離方式をRLS直接適用（anon_key+ユーザーJWT）に変更・設計書全体を整備 |
